<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme: dark background before Compose renders. Compose's SmartSaverTheme
         controls the real visual appearance; this XML theme only sets the window chrome. -->
    <style name="Theme.SmartSaver" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Share Activity: translucent so the caller app is visible through the ModalBottomSheet scrim -->
    <style name="Theme.SmartSaver.Share" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>
